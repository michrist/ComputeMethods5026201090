import java.util.Random;
public class ComputeMethods5026201090 {
	public double fToC (double degreesF) {
		double degreeC;
		degreeC = 5.0/9 * (degreesF-32);
		return degreeC;
	}
	public double hypotenuse(int a, int b){
		double c;
		c = Math.sqrt(Math.pow(a,2)+Math.pow(b,2));
		return c;
	}
	public int roll(){
		Random rndNumber = new Random();
		int diceValues = rndNumber.nextInt(11)+2;
		return diceValues;
	}
}
